html(lang="en")
    head
        meta(charset="UTF-8")
        title URL Shortener Microservice
        meta(name="description" content="URL Shortener Microservice")
        meta(name="author" content="Rumpel78")
        meta(name="viewport" content="width=device-width, initial-scale=1.0")
        link(rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.6/css/bootstrap.min.css")
        link(rel="stylesheet prefetch" href="https://maxcdn.bootstrapcdn.com/font-awesome/4.4.0/css/font-awesome.min.css")
    body
        div(class="container")
            h1(class="header") FreeCodeCamp API Basejump: URL Shortener Microservice
            blockquote
                p User stories:
                ul
                    li I can pass a URL as a parameter and I will receive a shortened URL in the JSON response.
                    li When I visit that shortened URL, it will redirect me to my original link.

            h3 Example creation usage:
            code #{host}/new/https://www.google.com
            a(href="new/https://www.google.com") [Show in browser]
            br
            
            code #{host}/new/http://foo.com:80
            a( href="new/http://foo.com:80") [Show in browser]

            h3 Example creation output
            code { "original_url":"http://foo.com:80", "short_url":"#{host}/H1YUseBux" }

            h3 Usage:
            code #{host}/H1YUseBux
            a(href="H1YUseBux") [Show in browser]

            h3 Will redirect to:
            code https://www.google.com/
            br
            br
            
            a(href=home) More freeCodeCamp apps